📁 داکیومنت پوشه app/Helpers
در این پوشه مجموعه‌ای از Helperهای کاربردی قرار دارد که برای سهولت نوشتن کد در تمام بخش‌های پروژه استفاده می‌شوند. هر Helper یک سری توابع سراسری (Global Functions) اضافه می‌کند.
________________________________________
🔐 1. auth_helper.php
کاربرد:
توابع مربوط به احراز هویت کاربر و گرفتن اطلاعات لاگین‌شده.
توابع مهم:
•	current_user() → برگرداندن شیء کاربر لاگین‌شده
•	is_logged_in() → بررسی ورود
•	is_admin() → بررسی سطح دسترسی
•	current_user_id() → گرفتن ID کاربر
•	کنترل سشن‌هایی مثل: user_id, logged_in, user_level, phone_verified
کاربرد عمومی:
در کنترلرها و ویوها برای شناسایی کاربر فعال.
________________________________________
🔄 2. convert_helper.php
کاربرد:
توابع تبدیل اعداد فارسی/عربی به انگلیسی.
توابع مهم:
•	fa_to_en()
•	ar_to_en()
کاربرد عمومی:
•	هنگام دریافت شماره موبایل
•	فیلدهای عددی فرم‌ها
•	ورودی‌هایی که ممکن است با کیبورد فارسی وارد شده باشند
________________________________________
🗓️ 3. date_helper.php
کاربرد:
توابع مدیریت تاریخ، تبدیل تاریخ، فرمت کردن DateTime و استفاده از کتابخانه jdf در صورت وجود.
کاربرد عمومی:
•	نمایش تاریخ جلالی
•	تبدیل زمان دیتابیس به زمان قابل‌خواندن
________________________________________
⚙️ 4. setting_helper.php
کاربرد:
گرفتن تنظیمات کلی سایت از جدول settings.
توابع مهم:
•	get_setting($name) → خواندن مقدار یک تنظیم
کاربرد عمومی:
•	گرفتن عنوان سایت، پیام‌ها، API keys، تنظیمات SMS، و…
________________________________________
⏳ 5. time_helper.php
کاربرد:
توابع محاسبه زمان باقی‌مانده، محاسبه اختلاف زمان، و تبدیل timestampها.
کاربرد عمومی:
•	نمایش زمان باقی‌مانده یک سرویس
•	شمارش معکوس انقضا
•	پردازش تاریخ‌های دقیق با CodeIgniter\I18n\Time

